# ===============================
# 
# ===============================
components:

  # ------------------
  # core
  # ------------------
  
  - name: core
    namespace: kube-system

    enabled: true
    priority: 0 

    syncPolicy:
      automated: false

    sources:

      - type: manifests
        repoURL: https://github.com/wsadza/homelab.git
        targetRevision: HEAD
        path: platform-products/infra-storage/applications/local-path-provisioner/components/core/manifests
