# ===============================
# 
# ===============================
applications:

  # ------------------
  # keycloak 
  # ------------------

  - name: keycloak 
    namespace: keycloak 

    enabled: true
    priority: 0

    syncPolicy:
      automated: false

    sources:

      #      - type: values
      #        repoURL: https://github.com/wsadza/homelab.git
      #        targetRevision: HEAD
      #        ref: values
      #
      #      - type: helm
      #        repoURL: https://codecentric.github.io/helm-charts
      #        targetRevision: 7.1.11
      #        chart: keycloakx
      #        valueFiles: 
      #          - $values/platform-products/platform-security/applications/keycloak/helm/values.yml

      - type: manifests
        repoURL: https://github.com/wsadza/homelab.git
        targetRevision: HEAD 
        path: https://raw.githubusercontent.com/keycloak/keycloak-k8s-resources/26.6.1/kubernetes/keycloaks.k8s.keycloak.org-v1.yml
