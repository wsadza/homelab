env:
  email: admin@anykey.pl
  password: admin

service:
  type: ClusterIP
  port: 80

persistence:
  enabled: true
  storageClass: storage-local-retain
  accessMode: ReadWriteOnce
  size: 1Gi

resources:
  requests:
    memory: "128Mi"
    cpu: "50m"
  limits:
    memory: "256Mi"
    cpu: "250m"
