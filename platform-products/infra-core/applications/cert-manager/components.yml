# ===============================
# 
# ===============================
components:

  # ------------------
  # core
  # ------------------

  - name: core
    sources:

      # -- Helper -----
      - type: values
        repoURL: https://github.com/wsadza/homelab.git
        targetRevision: HEAD
        ref: values

      # -- Release -----
      - type: helm
        repoURL: https://charts.jetstack.io
        targetRevision: 1.20.2 
        chart: cert-manager 
        valueFiles: 
          - $values/platform-products/infra-core/applications/cert-manager/components/core/helm/values.yml

