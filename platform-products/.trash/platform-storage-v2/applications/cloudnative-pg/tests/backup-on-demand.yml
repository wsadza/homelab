apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: psql-backup
  namespace: default 
spec:
  cluster:
    name: psql-instance
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
