# ===============================
# 
# ===============================
applications:

  # ------------------
  # Podinfo
  # ------------------

    - name: podinfo
      enabled: true
      namespace:
        autocreate: "true"
        labels:
          shared-s3-creds: "true"

  # ------------------
  # Something 
  # ------------------

  #    - name: something 
