{{- if and .Values.platformAuth.keycloak.enabled .Values.platformAuth.keycloak.users }}
{{- $root := . }}
{{- range .Values.platformAuth.keycloak.users }}
{{- if .enabled }}
{{- $secretName := default (printf "%s-secret" (.username | trunc 56 | trimSuffix "-")) .passwordSecret.name }}
{{- $secretKey := default "password" .passwordSecret.key }}
{{- $namespace := include "common.names.namespace" $root }}
{{- $existing := lookup "v1" "Secret" $namespace $secretName }}
{{- $existingValue := "" }}
{{- if and $existing $existing.data }}
{{- $existingValue = index $existing.data $secretKey }}
{{- end }}
{{- $generatedPassword := randAlphaNum 32 | b64enc }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName | quote }}
  namespace: {{ $namespace | quote }}
  labels:
    {{- include "common.labels.standard" $root | nindent 4 }}
type: Opaque
data:
  {{ $secretKey }}: {{ default $generatedPassword $existingValue | quote }}
{{- end }}
{{- end }}
{{- end }}
