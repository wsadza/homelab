# ===============================
# 
# ===============================
components:

  # ------------------
  # app
  # ------------------

    - name: app
      namespace: podinfo

      enabled: true
      priority: 0

      syncPolicy:
        automated: false

      sources:

        - type: values
          repoURL: https://github.com/wsadza/homelab.git
          targetRevision: HEAD
          ref: values

        - type: helm
          repoURL: https://stefanprodan.github.io/podinfo
          targetRevision: 6.9.1
          chart: podinfo 
          valueFiles: 
            - $values/platform-products/workload-tests2/applications/podinfo/components/app/helm/values.yml

  # ------------------
  # app-exposure
  # ------------------

    - name: app-exposure
      namespace: podinfo

      enabled: true
      priority: 0

      syncPolicy:
        automated: false

      sources:

        - type: values
          repoURL: https://github.com/wsadza/homelab.git
          targetRevision: HEAD
          ref: values

        - type: helm 
          repoURL: https://github.com/wsadza/homelab.git
          targetRevision: HEAD
          path: platform-charts/wrappers/app-platform
          valueFiles: 
            - $values/platform-products/workload-tests2/applications/podinfo/components/app-exposure/helm/values.yml

  # ------------------
  # app-security
  # ------------------

    - name: app-security
      namespace: podinfo

      enabled: false
      priority: 0

      syncPolicy:
        automated: false

      sources:

        - type: values
          repoURL: https://github.com/wsadza/homelab.git
          targetRevision: HEAD
          ref: values

        - type: helm 
          repoURL: https://github.com/wsadza/homelab.git
          targetRevision: HEAD
          path: platform-charts/wrappers/app-security
          valueFiles: 
            - $values/platform-products/workload-tests2/applications/podinfo/components/app-security/helm/values.yml

  # ------------------
  # app-auth
  # ------------------

    - name: app-auth
      namespace: podinfo

      enabled: true
      priority: 0

      syncPolicy:
        automated: false

      sources:

        - type: values
          repoURL: https://github.com/wsadza/homelab.git
          targetRevision: HEAD
          ref: values

        - type: helm 
          repoURL: https://github.com/wsadza/homelab.git
          targetRevision: HEAD
          path: platform-charts/wrappers/app-auth
          valueFiles: 
            - $values/platform-products/workload-tests2/applications/podinfo/components/app-auth/helm/values.yml
