
components:

  # ------------------
  # core
  # ------------------

  - name: core
    namespace: seaweedfs

    enabled: true
    priority: 0

    syncPolicy:
      automated: false

    sources:

      - type: values
        repoURL: https://github.com/wsadza/homelab.git
        targetRevision: HEAD
        ref: values

      - type: helm
        repoURL: https://seaweedfs.github.io/seaweedfs-operator
        targetRevision: 0.1.22 
        chart: seaweedfs-operator
        valueFiles: 
          - $values/platform-products/infra-storage/applications/seaweedfs-operator/components/core/helm/values.yml

      - type: manifests
        repoURL: https://github.com/wsadza/homelab.git
        targetRevision: HEAD
        path: platform-products/infra-storage/applications/seaweedfs-operator/components/core/manifests
