# ===============================
# 
# ===============================
applications:

  # ------------------
  # Nextcloud 
  # ------------------

    - name: nextcloud
      enabled: false

      namespace:
        name: nextcloud
        autocreate: "true"
        labels:
          shared-s3-creds: "true"

      syncPolicy:
        automated: false
